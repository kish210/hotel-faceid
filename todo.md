# فازبندی پروژه — سامانه هوشمند تشخیص چهره و مدیریت ورود/خروج هتل

> پروژه: تشخیص چهره مهمانان از دوربین‌های تحت شبکه Dahua / Hikvision،
> تخصیص شناسه یکتا، ثبت ورود و خروج، محاسبه شب‌های اقامت و نمایش
> لحظه‌ای تعداد نفرات حاضر در هتل.
>
> وضعیت: `[ ]` انجام‌نشده · `[~]` در حال انجام · `[x]` انجام‌شده

---

## فاز ۰ — اسکلت پروژه (Scaffold) — `[x]` انجام شد

- [x] ساختار مونوریپو (`services/api`، `services/face-service`، `web`، `db`)
- [x] اجرای بدون Docker: دو پروسهٔ پایتون (API + face-service)، پایگاه دادهٔ
      SQLite و سرو پنل build‌شده توسط خود API
- [x] اسکیمای پایگاه داده (`db/init/001_schema.sql`) شامل جداول
      `persons`، `face_embeddings`، `events`، `stays`، `cameras`، `users`
- [x] سرویس API با FastAPI: مدل‌ها، اسکیماها، لایه سرویس، روترها، WebSocket
- [x] منطق ورود/خروج، محاسبه اقامت و شمارش لحظه‌ای اشغال
- [x] موتور تشخیص چهره (RetinaFace + ArcFace از InsightFace)
- [x] لایه انتزاعی دوربین (ONVIF / Hikvision ISAPI / Dahua) + رجیستری برند
- [x] پنل وب React با داشبورد، لیست مهمانان، پروفایل، دوربین‌ها و گزارش‌ها
- [x] فایل `.env.example` و مستندات راه‌اندازی در `README.md`

### آزمون‌های انجام‌شده روی اسکلت

سامانه به‌صورت واقعی بالا آورده شد و این موارد تست و تأیید شد:

- [x] ورود به پنل با JWT و رد درخواست بدون توکن (۴۰۱)
- [x] احراز هویت جداگانه سرویس تشخیص چهره با `X-Service-Key`
- [x] تخصیص شناسه یکتا به چهره جدید
- [x] بازشناسی چهره تکراری (شباهت ۱.۰ ← همان شناسه، نه شناسه جدید)
- [x] ایجاد شناسه جدید برای چهره متفاوت (شباهت ۰.۰۸)
- [x] Debounce — تشخیص مجدد در بازه کوتاه رویداد تکراری نمی‌سازد
- [x] ثبت رویداد ورود/خروج و کاهش صحیح شمارنده اشغال پس از خروج
- [x] محاسبه شب اقامت (چک‌این ۳ روز پیش ← ۳ شب)
- [x] باز ماندن اقامت هنگام خروج کوتاه‌مدت مهمان
- [x] چک‌اوت خودکار پس از `STAY_TIMEOUT_HOURS` و ثبت نهایی تعداد شب
- [x] اشغال در لحظه گذشته (`?at=`)
- [x] ادغام دو شناسه تکراری با انتقال کامل رویدادها و اقامت‌ها
- [x] بروزرسانی زنده شمارنده حضور در پنل از طریق WebSocket (بدون رفرش صفحه)
- [x] رندر صحیح داشبورد، لیست مهمانان و گزارش‌ها با تقویم شمسی
- [x] خروجی Excel گزارش روزانه
- [x] لایه انتزاعی دوربین: ساخت آدرس RTSP اختصاصی Dahua و Hikvision،
      و بازگشت خودکار به ONVIF برای برند ناشناخته

> نکته: تست‌ها با بردار چهره ساختگی انجام شد. صحت‌سنجی دقت واقعی مدل
> روی چهره انسان، در **فاز ۳** انجام می‌شود.

---

## فاز ۱ — تحلیل و طراحی — `[~]`

- [~] بازدید میدانی از هتل و تهیه نقشه نقاط تردد (لابی، درب فرعی، پارکینگ)
- [ ] تعیین دقیق محل، ارتفاع (۲ تا ۲.۵ متر) و زاویه نصب هر دوربین (حداکثر ۱۵ درجه)
- [ ] فهرست‌برداری از دوربین‌های موجود: برند، مدل، فریم‌ور، پشتیبانی از ONVIF/ISAPI/NetSDK
- [ ] تعیین اینکه کدام دوربین‌ها موتور تشخیص چهره داخلی دارند (WizSense / DeepinView)
- [ ] تعیین سیاست نگهداری داده (Data Retention) و متن تابلوی اطلاع‌رسانی حریم خصوصی
- [ ] نهایی‌سازی مدل داده و تأیید سند طراحی فنی توسط کارفرما

**خروجی:** سند طراحی فنی نهایی + نقشه نصب دوربین‌ها

---

## فاز ۲ — راه‌اندازی زیرساخت — `[ ]`

- [ ] پیکربندی VLAN اختصاصی دوربین‌ها و تخصیص IP ثابت به هر دستگاه
- [ ] فعال‌سازی ONVIF و ساخت کاربر سرویس روی هر دوربین Dahua/Hikvision
- [ ] تست دریافت استریم RTSP از تک‌تک دوربین‌ها و ثبت آدرس‌ها در جدول `cameras`
- [ ] تهیه و راه‌اندازی سرور AI با GPU (NVIDIA + CUDA) و نصب درایورها
- [ ] در صورت انتخاب PostgreSQL به‌جای SQLite، راه‌اندازی سرور و اجرای `db/init/001_schema.sql`
- [ ] پیکربندی NVR / سیاست آرشیو ویدیوی خام
- [ ] راه‌اندازی بکاپ خودکار روزانه پایگاه داده

**خروجی:** زیرساخت آماده، اتصال همه دوربین‌ها تست‌شده

---

## فاز ۳ — توسعه هسته تشخیص — `[ ]`

- [ ] اتصال پایدار به استریم RTSP با مدیریت قطعی و اتصال مجدد خودکار
- [ ] تنظیم آستانه شباهت (similarity threshold) بر اساس داده واقعی هتل
- [ ] پیاده‌سازی کنترل کیفیت چهره: حداقل اندازه، تاری، زاویه سر، نور
- [ ] پیاده‌سازی تشخیص جهت تردد (Line Crossing / نقش دوربین ورودی-خروجی)
- [ ] پیاده‌سازی Debounce برای جلوگیری از ثبت رویداد تکراری در چند فریم پیاپی
- [x] دریافت رویدادهای تشخیص چهره داخلی دوربین از Hikvision ISAPI و Dahua
      NetSDK (لایه دوربین مطابق مستندات رسمی SDK تنظیم شد: پارس boundary و
      `facePicURL` در Hikvision؛ پارس `Code=...;data={JSON}` و `snapshot.cgi`
      در Dahua) + تست‌های واحد در `services/face-service/tests/test_cameras.py`
- [x] پشتیبانی از برند **Axis**: آداپتر `cameras/axis.py` (RTSP روی
      `axis-media/media.amp` و رویدادهای آنالیتیکس از طریق ONVIF PullPoint)
      + تست‌های واحد
- [x] تشخیص خودکار برند/مدل/فریم‌ور دوربین از روی خود دستگاه
      (`services/api/app/services/camera_probe.py`، `POST /api/cameras/probe`)
- [x] تشخیص جنسیت و سن تخمینی با مدل `genderage` و تصمیم‌گیری با رأی اکثریت
      روی مشاهدات (`recognition.update_gender`) + امکان اصلاح دستی توسط اپراتور
- [ ] تست دقت روی حداقل ۵۰۰ تردد واقعی و رسیدن به دقت هدف (≥ ۹۵٪)
- [ ] سنجش دقت تشخیص جنسیت روی داده واقعی و کالیبره کردن `GENDER_MIN_QUALITY`
- [ ] پیاده‌سازی ادغام دستی دو شناسه (merge) در صورت خطای تشخیص

**خروجی:** سرویس تشخیص عملیاتی با دقت قابل قبول

---

## فاز ۴ — توسعه پنل و گزارش‌گیری — `[x]` انجام شد

- [x] احراز هویت کاربران پنل (JWT) و کنترل دسترسی نقش‌محور (RBAC)
- [x] تکمیل داشبورد لحظه‌ای: شمارنده حضور، نمودار ورود/خروج ۲۴ ساعته
- [x] جست‌وجوی تصویری مهمان بر اساس آپلود عکس
- [x] صفحه مدیریت دوربین‌ها با نمایش وضعیت اتصال زنده، **ویرایش دوربین** و
      دکمه «تشخیص خودکار مدل دوربین»
- [x] نمایش جنسیت در فهرست مهمانان، پروفایل فرد و شمارندهٔ آقایان/بانوان در داشبورد
- [x] گزارش اشغال در ساعت دلخواه از روزهای گذشته
- [x] گزارش مهمانان با بیشترین شب اقامت (مهمانان وفادار)
- [x] خروجی Excel و PDF از تمام گزارش‌ها
- [x] ارسال خودکار گزارش دوره‌ای از طریق ایمیل
- [x] فارسی‌سازی کامل رابط کاربری و تقویم شمسی
- [x] صفحه مدیریت کاربران (ایجاد/ویرایش/حذف/تغییر رمز)

**خروجی:** پنل وب کامل و قابل استفاده

---

## فاز ۵ — امنیت و حریم خصوصی — `[~]`

- [ ] رمزنگاری تصاویر و بردارهای چهره در حالت ذخیره (At Rest)
- [ ] اجباری‌کردن HTTPS/TLS روی تمام سرویس‌ها (In Transit)
- [x] پیاده‌سازی Audit Log برای هر دسترسی و خروجی‌گیری اپراتور
- [x] اجرای خودکار سیاست حذف تصاویر خام پس از ۳۰ تا ۹۰ روز
- [x] پیاده‌سازی حذف کامل اطلاعات یک فرد (Right to be Forgotten)
- [ ] نصب تابلوی اطلاع‌رسانی وجود سامانه تشخیص چهره در ورودی‌ها
- [ ] تست نفوذ و بازبینی امنیتی سامانه

**خروجی:** سامانه منطبق با الزامات حریم خصوصی

---

## فاز ۶ — تست و راه‌اندازی نهایی — `[ ]`

- [ ] تست یکپارچه در محیط واقعی هتل به مدت حداقل دو هفته
- [ ] تست بار (Load Test) با حداکثر تعداد دوربین و تردد همزمان
- [ ] صحت‌سنجی شمارنده اشغال در برابر شمارش دستی
- [ ] آموزش کاربران: پذیرش، حراست، مدیریت
- [ ] تهیه مستندات کاربری و دستورالعمل عیب‌یابی
- [ ] تعریف مانیتورینگ و هشدار (قطعی دوربین، پر شدن دیسک، خطای سرویس)
- [ ] تحویل نهایی و شروع دوره پشتیبانی

**خروجی:** سیستم در حال بهره‌برداری

---

## فاز ۷ — توسعه‌های آتی (اختیاری) — `[ ]`

- [ ] یکپارچه‌سازی با سیستم رزرواسیون هتل (PMS) و تطبیق چهره با رزرو
- [ ] کنترل تردد فیزیکی: باز کردن گیت/درب برای مهمان شناسایی‌شده
- [ ] اپلیکیشن موبایل برای مدیریت
- [ ] تشخیص تخلف: ورود فرد ثبت‌نشده در ساعات غیرمجاز
- [ ] تحلیل مسیر حرکت (Path Analytics) در فضاهای عمومی هتل
- [ ] لیست سیاه/سفید و هشدار لحظه‌ای هنگام تشخیص فرد خاص

---

## انتشار ۱.۲.۰ — بستهٔ نصب خودکفا — `[x]` انجام شد

- [x] ساخت بسته نصب ویندوزی با **Inno Setup** (`setup/Hotel-FaceID.iss`)
- [x] فایل نسخه (`setup/VERSION` = 1.2.0)
- [x] **بستهٔ خودکفا**: موتور پایتون قابل‌حمل، همهٔ کتابخانه‌ها و مدل‌های
      InsightFace داخل نصب‌کننده — روی سیستم مقصد هیچ پیش‌نیازی (حتی اینترنت)
      لازم نیست
- [x] اسکریپت ساخت بسته (`setup/build.ps1`): مونتاژ runtime، دانلود یک‌بارهٔ
      مدل‌ها، build پنل و کامپایل Inno Setup
- [x] اسکریپت راه‌اندازی (`setup/scripts/install.ps1`): ساخت `.env` با کلیدهای
      تصادفی، قانون فایروال، اجرای هر دو سرویس و باز کردن پنل در مرورگر
- [x] میان‌برهای فارسی «شروع / توقف / وضعیت / گزارش خطا» + اجرای خودکار
      هنگام روشن شدن ویندوز (اختیاری)
- [x] اسکریپت‌های کمکی: `start.ps1` / `stop.ps1` / `status.ps1` / `logs.ps1` /
      `debug.ps1` (جمع‌آوری لاگ برای عیب‌یابی)
- [x] سورس کامل سامانه داخل بسته نصب کپی می‌شود تا امکان debug مستقیم باشد
- [x] خروجی: `setup/dist/Hotel-FaceID-Setup-1.2.0.exe` (انتشار GitHub: `v1.2.0`)
